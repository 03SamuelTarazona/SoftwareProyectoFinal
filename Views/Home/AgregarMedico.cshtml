﻿@{
    Layout = null;
}


@{
    
    Software_Proyecto.Dto.PersonaDto personalogueo = Session["AdminLogged"] as Software_Proyecto.Dto.PersonaDto;
}

@if (personalogueo == null)
{
    <!DOCTYPE html>
    <html>
    <head>
        <title>Redirección automática en ASP.NET</title>
        <script type="text/javascript">
            window.onload = function () {
                setTimeout(function () {
                    window.location.href = 'Index';
                });
            };
        </script>
    </head>
    <body>
        <h1>Inicie Sesion primero</h1>
    </body>
</html>
}
else
{
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Medico</title>
    <link href="~/Content/AgregarMedico.css" rel="stylesheet" />
</head>
<body>

    <div class="container">

        @using (Html.BeginForm("AgregarMedico", "Home", FormMethod.Post))
        {
            @Html.AntiForgeryToken()


            <h2>Registro del Médico</h2>

            <div class="form-group">
                <label for="nombres">Nombres:</label>
                <input type="text" id="persona.nombres" name="persona.nombres" required>
            </div>

            <div class="form-group">
                <label for="apellidos">Apellidos:</label>
                <input type="text" id="persona.apellidos" name="persona.apellidos" required>
            </div>

            <div class="form-group">
                <label for="documento">Documento:</label>
                <input type="text" id="persona.documento" name="persona.documento" required>
            </div>

            <div class="form-group">
                <label for="fecha_nacimiento">Fecha de Nacimiento:</label>
                <input type="date" id="persona.fecha_nacimiento" name="persona.fecha_nacimiento" required>
            </div>

            <div class="form-group">
                <label for="persona.genero">Género:</label>

                <select id="persona.genero" name="persona.genero" required>
                    <option value="null">Seleccione</option>
                    <option value="femenino">Femenino</option>
                    <option value="masculino">Masculino</option>
                </select>
            </div>

            <div class="form-group">
                <label for="correo">Correo:</label>
                <input type="email" id="persona.correo" name="persona.correo" required>
            </div>

            <div class="form-group">
                <label for="contrasena">Contraseña:</label>
                <input type="password" id="persona.contrasena" name="persona.contrasena" required>
            </div>

            <div class="form-group">
                <label for="especialidad">Especialidad:</label>
                <input type="text" id="especialidad" name="especialidad" required>
            </div>

            <div class="containerbutton">
                <button type="submit" onclick="crearCuenta()">Crear</button>
            </div>

    

    }  @if (ViewData["Mensaje"] != null)
        {
            <p>@ViewData["Mensaje"]</p>
        }
    <div class="containerbutton">

        <button  onclick="cancel()">Cancelar</button>
    </div>
    </div>
    <script>
        function crearCuenta() {
            window.alert("Cuenta creada con exito");

        }
    </script>
    <script>
        function cancel() {
           window.location.href = '@Url.Action("ListaMedicos", "Home")';
        }
    </script>
</body>
</html>
}